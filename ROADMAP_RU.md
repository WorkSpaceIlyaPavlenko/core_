# План развития проекта

Core изначально проектируется не как законченная библиотека, а как ядро экосистемы.
Ниже представлено целостное видение дальнейшего развития.

---

## 🔧 DevKit (Developer Kit)

DevKit — инструментарий для разработки конфигураций, а не часть runtime.

### 🎯 Цели DevKit
- помощь при написании AST
- контроль id / kind
- предотвращение конфликтов
- удобная работа с сигналами
- будущая IDE-интеграция

### 📦 Текущее состояние

Сейчас DevKit:
- хранит список зарегистрированных `{ id, kind }`
- используется при разработке конфигураций
- не участвует в runtime
- не попадает в production

```ts
items.register({ id: 'button_1', kind: 'button' })
```

### 🔮 Планируемые возможности DevKit

#### 1. AST Index
- глобальный реестр всех элементов
- проверка уникальности id
- быстрый поиск по kind / owner

#### 2. IDE-подсказки
- автодополнение `targetId` в `behavior.emit`
- подсказка допустимых `elKind`
- подсказка `payload` по типу сущности

#### 3. Dev Warnings
Предупреждения о:
- несуществующих `targetId`
- несовместимых `payload`
- неиспользуемых сигналах

#### 4. AST Inspector
- визуализация дерева
- просмотр связей сигналов
- отладка поведения

---

## 🧩 Расширение движка (Engine)

### 🔹 Улучшенный парсер
- оптимизированный DFS
- поддержка partial-run
- hot-reparse только изменённых веток

### 🔹 Контейнеры
- сложные layout-контейнеры
- grid / flow / virtual
- lazy-children

### 🔹 Расширение behavior
- системные сигналы
- условные emits
- chained emits

---

## 🔔 Signals & Handlers

Планируется:
- middleware для сигналов
- async-handlers
- debounce / throttle
- transactional emits
- dev-лог сигналов

```
UI Event
   ↓
Signal
   ↓
Handler
   ↓
Store patch
```

---

## 🧠 Runtime-оптимизации
- batching обновлений
- memo-selectors
- selective re-render
- tree-diff без React reconciliation

---

## 🏗 Builder App

### 🎯 Идея

Отдельное web-приложение, использующее Cubro Core как движок.
Это не часть core, а consumer-приложение.

### Возможности Builder App

#### 🖱 Визуальный редактор
- drag & drop элементов
- редактирование props
- управление children

#### 🔗 Редактор сигналов
- визуальное связывание элементов
- настройка emits
- просмотр цепочек событий

#### 🌳 AST Tree View
- дерево компонентов
- навигация по id
- быстрый jump к узлу

#### 🧪 Preview Runtime
- мгновенный runtime-рендер
- проверка сигналов
- live-обновления

### Архитектура Builder App

```
Builder UI
   ↓
AST Editor
   ↓
Cubro Core
   ↓
Preview Renderer
```

Core остаётся чистым и универсальным,
Builder — один из клиентов.

---

## 📦 Экосистема

```
┌───────────────┐
│   DevKit      │
└───────┬───────┘
        ↓
┌───────────────┐
│  Cubro Core   │
└───────┬───────┘
        ↓
┌───────────────┐
│ Builder App   │
└───────┬───────┘
        ↓
┌───────────────┐
│ Production UI │
└───────────────┘
```

---

## 🧠 Философия проекта
- Core — минимальный
- DevKit — мощный
- Builder — опциональный
- Runtime — строгий
- UI — полностью декларативный

---

## 🏁 Заключение

Cubro — это не «очередной UI-фреймворк»,
а фундамент для декларативных, расширяемых UI-систем.
